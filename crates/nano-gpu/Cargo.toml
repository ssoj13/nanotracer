[package]
name        = "nano-gpu"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Vulkan runtime (ash + shaderc) and Scene -> GPU buffer marshalling for nanotracer-rs."

[dependencies]
nano-core = { workspace = true }
glam      = { workspace = true }
ash       = { workspace = true }
shaderc   = { workspace = true }
bytemuck  = { workspace = true }
